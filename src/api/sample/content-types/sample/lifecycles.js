module.exports = {
    // afterFindOne(event) {
    //     const { result, params } = event;
    //     result.organ = JSON.parse(result.organ)
    // },
    // afterFindMany(event) {
    //     const { result, params } = event;
    //     result.forEach(function (item, index) {
    //         item.organ = JSON.parse(item.organ)
    //     });
    // }
}